name: CI
on:
  pull_request:
  push:
    branches: [main]
    tags: ['v*']
jobs:
  ci:
    uses: stampede-io/.github/.github/workflows/service-ci.yml@main
    with:
      service-name: catalog
    secrets: inherit
